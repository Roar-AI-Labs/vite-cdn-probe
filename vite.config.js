import { defineConfig } from 'vite';

// Deliberately states no `base`. That is the ordinary case and the one the
// platform's `--base` flag exists for: a config it must not need to edit.
export default defineConfig({});
